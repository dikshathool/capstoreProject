import { MerchantProductSearchPipe } from './merchant-product-search.pipe';

describe('MerchantProductSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MerchantProductSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
